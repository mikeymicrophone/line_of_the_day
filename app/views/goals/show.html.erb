<%= @goal.describe %><br>
<%= @goal.repetitions %> times<br>
<%= @goal.days %> days<br>
<%= link_to 'do it', goal_ownerships_path(:goal_ownership => {:goal_id => @goal.id, :user_id => current_user.id}), :method => :post %>