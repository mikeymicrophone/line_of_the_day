<% list_items.each do |item| %>
  <% div_for item.item do %>
	  <%= item.item.name %>
  <% end %>
  <script type='text/javascript'>
		new Draggable(<%= dom_id(item.item) %>, {revert: true, scroll: window});
  </script>
<% end %>
