<%= link_to image_tag('top_rated.png'), {:sort => 'rating'}, :class => 'sort' %>
<%= link_to image_tag('randomize.png'), {:sort => 'random'}, :class => 'sort' %>
<h1>some of the greatest things young ladies have yet heard</h1>
<div class='clearfix'></div>

<div id="line_bin">
  <%= render :partial => @published_lines if @published_lines %>
  <hr>

  <%= render :partial => @public_lines if @public_lines %>

  <hr>

  <%= render :partial => @shared_lines if @shared_lines %>

  <%= will_paginate @public_lines if @public_lines %>
</div>
<%= link_to 'drop a line', new_line_path %>

<div id='publishable-groups'></div>
<i>choose which group(s) to publish to by clicking them!</i>
<% current_user.andand.cliques.andand.each do |g| %>
  <%= link_to_function g.name, "make_publishable(#{g.id});" %>
<% end %>
<div id='publications'></div>
<%= render :partial => Comment.main %>
<%= slider_stylesheet %>